[project]
name = "handler-common"
version = "0.1.0"
description = "Shared utilities for Handler packages"
requires-python = ">=3.11"
dependencies = [
    "rich>=13.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "handler-app"
version = "0.1.0"
description = "Handler TUI application"
requires-python = ">=3.11"
dependencies = [
    "handler-client",
    "textual>=0.47.0",
    "httpx>=0.27.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
